type TStyle = { page: React.CSSProperties };

const styles: TStyle = {
  page: {
    alignItems: 'center',
    justifyContent: 'center',
    height: '100vh',
    padding: '80px 100px',
  },
};

export default styles;
